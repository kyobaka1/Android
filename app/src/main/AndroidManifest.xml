<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dev.the_food_house">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity"
            android:parentActivityName=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".Setting_Fragment"
        android:parentActivityName=".MainActivity"
        android:label="                         Cài đặt">
         </activity>
        <activity android:name=".Setting_Fragment_Quanlitaikhoan"
            android:parentActivityName=".Setting_Fragment"
            android:label="              Quản lí tài khoản">
        </activity>
        <activity android:name=".Setting_Fragment_Thanhtoan"
            android:parentActivityName=".Setting_Fragment"
            android:label="                 Thanh toán">
        </activity>
        <activity android:name=".Setting_Fragment_Hotro"
            android:parentActivityName=".Setting_Fragment"
            android:label="                   Hỗ trợ">
        </activity>
        <activity android:name=".Setting_Fragment_Gioithieu"
            android:parentActivityName=".Setting_Fragment"
            android:label="                 Giới thiệu">
        </activity>

    </application>

</manifest>